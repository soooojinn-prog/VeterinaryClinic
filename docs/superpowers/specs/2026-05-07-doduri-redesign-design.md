# 도두리동물병원 사이트 리디자인 v2 — 설계서

- **작성일:** 2026-05-07
- **대상 도메인:** https://doduri-ah.co.kr
- **결정 근거:**
  - 클라이언트 PDF (`도두리 동물병원 프로젝트_0504.pdf`) — 최신 협의 내용, 최우선
  - 구글 스프레드시트 요구사항 명세서 (`1_65fQ761auDnoEvMr9tn1x_MkCRDn6SZV4gXLGtySDY`) — 일반론, 보조

## 1. 배경 / 변경 동기

기존 v1 (`master` 현재 상태)은 정적 HTML 7페이지를 WordPress 테마로 1:1 이식한 결과물이다. 이후 클라이언트(원장님)와의 4차 협의(2026-04-13, 04-27, 05-04)에서 다음이 확정되어 메뉴·콘텐츠 구조 재구성이 필요하다.

핵심 변경:
1. **`상담/예약` 탭 폐지 → `커뮤니티` 탭(공지사항 + Q&A) 신설**
2. **메인 페이지 — 정적 배경 1장 → 병원사진 슬라이드 + 공지 미리보기**
3. **푸터 사업자번호(`409-26-52253`) 추가, 카카오톡 채널 연동**
4. **오시는길 — 지하철 항목 제거, 버스 정류장 텍스트 갱신, 카톡 채널 버튼 추가**
5. **사이드 플로팅 버튼 — 전화/카톡/블로그 (인스타·유튜브 제외)**

## 2. 사이트맵 (확정)

```
도두리동물병원 (https://doduri-ah.co.kr)
│
├─ /  홈 (front-page.php)
│
├─ 병원소개
│   ├─ /greeting/   인사말
│   ├─ /doctor/     의료진소개 (단일)
│   └─ /facility/   시설
│
├─ 진료안내
│   ├─ /service-subject/   진료과목 (3개 클리닉 단일 페이지)
│   ├─ /location/#hours    진료시간 (앵커, 별도 페이지 없음)
│   └─ /location/          오시는 길
│
└─ 커뮤니티 (← 상담/예약 변경)
    ├─ /notice/    공지사항 (KBoard, 관리자 전용)
    └─ /qna/       Q&A (KBoard, 비회원 작성·비밀글)
```

### 명시적 제외 (시트엔 있으나 PDF 협의에 없음)

- 병원특장점 페이지 (REQ-010)
- 진료과목 상세 페이지 분리 (REQ-013) → 단일 페이지 유지
- 치료 케이스 게시판 (REQ-015)
- FAQ (REQ-016)
- SNS 피드 위젯 / Smash Balloon (REQ-017)
- 의료진 그리드 다중 (REQ-008) → 김한민 원장 단일 유지

## 3. 페이지별 변경 내역

### 3.1 유지 (콘텐츠·구조 그대로)

| 페이지 | 비고 |
|---|---|
| `/greeting/` 인사말 | PDF p.5 콘텐츠 적용 완료 |
| `/doctor/` 의료진소개 | PDF p.8 콘텐츠 적용 완료 |
| `/facility/` 시설 | 실제 병원사진 5장 적용 완료 |
| `/service-subject/` 진료과목 | PDF p.13 3개 클리닉 적용 완료 |

### 3.2 수정

| 파일 | 변경 |
|---|---|
| `header.php` | 메뉴 구조 — 병원소개 / 진료안내 / 커뮤니티. 진료시간 메뉴 항목은 `/location/#hours` 앵커 |
| `inc/menus.php` | 폴백 메뉴 동기화 (커뮤니티 추가) |
| `inc/theme-setup.php` | body_class — `page-contact` → `page-community` |
| `front-page.php` | 히어로 정적 배경 → 병원사진 3~5장 슬라이드 자동재생. 공지 최신글 미리보기 + 진료시간/오시는길 요약 박스 추가 |
| `footer.php` | 사업자번호 (`site_biz_no` ACF 값 출력) 추가. SNS 아이콘: 블로그 + 카톡 (인스타·유튜브 제외) |
| `page-location.php` | 지하철 항목 제거. 버스 정류장 텍스트 갱신 ("계산중학교 / 부평동초등학교 / 대동아파트 도보 5분 이내"). 진료시간 섹션에 `id="hours"` 앵커. 카톡 채널 버튼(`http://pf.kakao.com/_lwlTX`). 네이버지도 URL → `https://naver.me/5nhPYsQU` |

### 3.3 삭제

| 파일/리소스 | 이유 |
|---|---|
| `page-contact.php` | 상담/예약 페이지 폐지 |
| WP 페이지 `상담/예약` (slug `contact`) | 휴지통 처리 (이미 완료) |

### 3.4 신규

| 파일 | 역할 |
|---|---|
| `page-notice.php` | 공지사항 페이지 템플릿 — 서브 헤더 + 커뮤니티 탭 + `the_content()` (KBoard 숏코드 자동 출력) |
| `page-qna.php` | Q&A 페이지 템플릿 — 위와 동일 구조 |
| `template-parts/home-notices.php` | 메인 페이지 공지 최신 2~3건 위젯 (KBoard API로 조회) |
| `template-parts/floating-buttons.php` | 사이드 플로팅 버튼 (전화/카톡/블로그) — 모든 페이지 공통 |
| `assets/css/kboard-override.css` | KBoard 기본 스킨 도두리 톤 통일 |
| `assets/css/floating-buttons.css` | 플로팅 버튼 스타일 |
| WP 페이지 `공지사항` (slug `notice`) | 본문에 KBoard 숏코드 입력. 템플릿: `page-notice.php` |
| WP 페이지 `QnA` (slug `qna`) | 본문에 KBoard 숏코드 입력. 템플릿: `page-qna.php` |

## 4. 메인 페이지 구성 (C안 확정)

```
1. 히어로 슬라이드 — 병원사진 3~5장 자동재생 (3~5초 간격, fade transition)
2. 진료과목 3개 클리닉 미니카드 — 건강검진 / 내과 / 순환기·시니어
3. 공지사항 최신 미리보기 — 2~3건 (썸네일 + 제목 + 날짜)
4. 진료시간 / 오시는길 요약 박스 — 시간 + 주소 + 전화 + 지도/카톡 버튼
5. 푸터
```

ACF 옵션 신설:
- `home_hero_slides` (image array) — 비어있을 때 facility1~5.png 사용

## 5. 커뮤니티 게시판 — KBoard 설정 상세

### 5.1 플러그인
- KBoard (무료 버전) 설치 → 활성화

### 5.2 공지사항 게시판
| 항목 | 값 |
|---|---|
| 이름 | 도두리 공지사항 |
| 글쓰기 권한 | 관리자만 |
| 비회원 작성 | OFF |
| 첨부파일 | 이미지만, 최대 3개, 2MB/파일 |
| 댓글 | OFF |
| 비밀글 | OFF |
| 페이지당 글 수 | 10 |

### 5.3 Q&A 게시판
| 항목 | 값 |
|---|---|
| 이름 | 도두리 Q&A |
| 글쓰기 권한 | 누구나 (비회원 포함) |
| 비회원 작성 | ON (이름 + 비밀번호 필수) |
| 비밀글 | ON (체크박스로 선택) |
| 첨부파일 | OFF |
| 댓글 | ON (KBoard 댓글, 관리자 답글용) |
| 스팸 방지 | KBoard 자체 캡차 (1단계) — 스팸 발생 시 reCAPTCHA로 업그레이드 |
| 욕설 필터 | ON |
| 페이지당 글 수 | 15 |

### 5.4 댓글 결정
**KBoard 댓글 사용** (WP 기본 댓글 미사용). 사유: 비밀글/비회원 답글 등 게시판 통합성, PDF 요구사항 부합도.

## 6. 푸터 / 플로팅 버튼

### 6.1 푸터
- 병원명 / 대표자 / 사업자번호 (`409-26-52253`) / 주소 / 전화 / 이메일
- SNS 아이콘: 블로그(`blog.naver.com/doduri_ah`) / 카톡 채널(`http://pf.kakao.com/_lwlTX`)
- 인스타·유튜브 제외 (PDF 명시)

### 6.2 사이드 플로팅 버튼
- 전화 (`tel:032-545-7582`)
- 카톡 채널 (`http://pf.kakao.com/_lwlTX`)
- 블로그 (`blog.naver.com/doduri_ah`)
- 모바일에선 화면 우측 하단, PC에선 우측 중앙

## 7. ACF 옵션 변경

신규:
- `home_hero_slides` (image array) — 메인 히어로 슬라이드

값 확정 (필드 자체는 project_theme.md 에 이미 대기 상태로 등록됨):
- `site_biz_no` ← `409-26-52253`

값 변경:
- `site_kakao_url` ← `http://pf.kakao.com/_lwlTX` (대기 → 확정)
- `naver_map_url` ← `https://naver.me/5nhPYsQU`

## 8. 작업 순서 (마일스톤)

| STEP | 내용 | 비고 |
|---|---|---|
| 1 | 코드 베이스 정리 (메뉴/푸터/플로팅) | 로컬 |
| 2 | 메인 페이지 보강 (히어로 슬라이드 + 공지 미리보기 + 요약 박스) | 로컬 |
| 3 | 오시는길 페이지 미세 조정 | 로컬 |
| 4 | 커뮤니티 페이지 템플릿 신설 (page-notice.php, page-qna.php) | 로컬 |
| 5 | KBoard 설치 + 게시판 셋업 + 숏코드 입력 + CSS 오버라이드 | 로컬 |
| 6 | 사진 자료 적용 (Drive 폴더 다운로드 후 ACF 등록) | 로컬 |
| 7 | GitHub 레포 + .gitignore 셋업 | task #7 |
| 8 | GitHub Actions CI/CD 워크플로우 | task #8 |
| 9 | 첫 배포 검증 + 라이브 KBoard 셋업 | task #9 |
| 10 | 보안 감사 + QA | task #10 |
| 11 | 클라이언트 인계 매뉴얼 | task #11 |

## 9. 외부 자원 / 의존

- 카카오톡 채널: `http://pf.kakao.com/_lwlTX`
- 네이버 지도 short URL: `https://naver.me/5nhPYsQU`
- 네이버 블로그: `blog.naver.com/doduri_ah`
- 네이버 NCP Maps API (VPC): Client ID `goa90mkxm6`
- 사진 자료: PDF p.24 Drive 폴더 (다운로드 필요)
- 사업자번호: `409-26-52253`
- 전화번호: `032-545-7582` (개통 예정)
- 이메일: `doduri_ah@naver.com`

## 10. 검증 기준 (Done 정의)

각 STEP 완료 시:
- 로컬(`http://doduri.local`)에서 시각적 회귀 없이 동작
- 모바일/태블릿/데스크톱 반응형 동작
- 진료시간 메뉴 클릭 시 `/location/#hours` 스크롤 정상
- KBoard 비회원 작성 + 비밀글 + 관리자 답글 사이클 성공
- 공지 작성 → 메인 미리보기 자동 반영
- 네이버 지도 정상 로드 (NCP 화이트리스트 매칭)
- 푸터 사업자번호·플로팅 버튼 모든 페이지 노출

## 11. 위험 / 미정 항목

- **사진 자료 다운로드 권한** — Drive 폴더 링크 접근 가능 여부 (작업 시 확인)
- **카카오톡 채널 활성화 시점** — PDF 시점 "예정"이었음, 현재 활성 상태 확인 필요
- **전화번호** — "032-545-7582 가능성 높음"이라 했음, 변경 가능성 대비해 ACF 옵션으로 관리
- **KBoard 디자인 충돌** — 무료 스킨이 도두리 톤(베이지/워머)과 안 맞을 수 있어 CSS 오버라이드 필요. 분량은 실제 화면 보고 결정.

## 12. 비고

- 본 설계서는 PDF (2026-05-04 기준) 우선. 시트와 충돌 시 PDF 우선.
- 시트의 1순위 요구사항이지만 PDF 협의에 없는 항목은 v3에서 검토 (병원특장점, 진료과목 상세 분리 등).
- 디자인 톤(베이지/워머)은 PDF 답변에서 "괜찮다"는 컨펌을 받았으므로 유지.
